[build-system]
requires = ["setuptools>=77.0", "wheel", "cffi>=1.15.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tachywooting"
version = "0.2.3"
description = "Python interface for Wooting analog keyboards"
readme = "README.md"
requires-python = ">=3.9,<3.15"
license = "BSD-3-Clause"
license-files = ["LICENSE"]
authors = [
    {name = "Mathias Salvas-Hébert"}
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Operating System :: MacOS",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
keywords = ["wooting", "analog-keyboard", "keyboard", "cffi", "hdf5"]

dependencies = [
    "cffi>=1.15.0",
    "h5py>=3.8.0",
    "matplotlib>=3.6.0",
    "numpy>=1.23.0",
    "pandas>=1.5.0",
    "rich>=10.0.0",
    "setuptools>=77.0",
    "tqdm>=4.60.0",
    "patchelf>=0.17.2; sys_platform == 'linux'",
]

[project.optional-dependencies]
docs = [
    "furo>=2024.8.6",
    "sphinx>=7.0.0",
]
dev = [
    "black>=23.0.0",
    "build>=1.0.0",
    "pytest>=7.0.0",
    "roman>=4.0",
    "ruff>=0.4.0",
    "sphinx>=7.0.0",
    "furo>=2024.8.6",
    "twine>=5.0.0",
]
all = [
    "tachywooting[dev]",
]

[tool.setuptools.packages.find]
include = ["tachywooting*"]
namespaces = true

[tool.setuptools.package-data]
tachywooting = [
    "libraries/**/*",
    "permissions/**/*",
]


[tool.setuptools.exclude-package-data]
tachywooting = [
    "libraries/**/debug/*",
    "libraries/**/VIRTUAL_KEYBOARD.md",
    "libraries/**/wooting-analog-virtual-control",
    "libraries/**/wooting-analog-virtual-control.exe",
]

[project.scripts]
wooting-demo = "tachywooting.demos.cli:main"
wooting-build-interface = "tachywooting.package_setup:run_post_install"
wooting-delete-interface = "tachywooting.package_setup:main_delete_interface"
wooting-visualize = "tachywooting.visualize:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = ["B", "E", "F", "I", "UP"]
ignore = ["E501"]
